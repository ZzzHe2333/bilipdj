# 弹幕排队姬 v2.0.2

v2.0.2 是 v2.0.1 的稳定性与备份能力增强版本，重点加入“只备份设置”的多后端备份，并修复备份 UI、目标切换一致性和并发覆盖问题。

## 本次提供的发行包

### Windows Tk 便携版

文件：`BiliPDJ-v2.0.2-Windows-Tk-Portable-x64.zip`

完整解压后双击 `main.exe`：

- 自动启动内置后端；
- 不需要安装 Python；
- 自带 Bilibili / 抖音后端、Web 静态资源、OBS 透明窗口和更新组件；
- Bilibili 扫码登录使用原生桌面二维码弹窗；
- 设置页新增独立可滚动“数据备份”子页；
- 关闭桌面程序时，它启动的后端会一并停止。

### Web 便携版

文件：`BiliPDJ-v2.0.2-Web-Portable-x64.zip`

完整解压后双击 `BiliPDJ-Web.exe`：

- 自动检查并启动内置后端；
- 等待 `/health` 就绪后自动打开本地 Web 管理页；
- 可从启动器再次打开管理页或队列看板；
- 点击“停止并退出”只关闭本启动器自己拉起的后端；
- 不需要安装 Python，也不需要手工运行 Server 命令。

## 新增：设置备份

支持三种备份目标：

- WebDAV；
- 本地文件夹；
- SMB / NAS（使用操作系统已认证或已挂载的路径，不在 BiliPDJ 内保存 NAS 密码）。

备份范围固定为实际存在的以下设置文件：

- `config.yaml`
- `quanxian.yaml`
- `kaiguan.yaml`
- `style.json`

不会备份队列、黑名单、日志、WebDAV 密码或 NAS 凭据。ZIP 会保留每个设置文件的最后修改时间，恢复时同步恢复该时间。

支持：立即备份、历史列表、恢复、启动时自动备份、退出时自动备份、保留最近 N 份。

## 修复

- 修复 Tk 备份面板挤压设置页导致的小窗口/DPI 错位问题，备份功能改为独立可滚动“数据备份”页；
- 修复切换 WebDAV / 本地 / SMB-NAS 后刷新或恢复仍可能操作旧目标的问题；
- 切换后端或修改路径后旧历史列表自动失效，恢复前再次校验目标；
- 备份/恢复/清理改为共享互斥，避免不同请求并发踩数据；
- 备份名增加微秒级冲突规避，解决同一秒连续或并发备份覆盖；
- 统一历史备份时间显示为本地 `YYYY-MM-DD HH:MM:SS`；
- 修复 WebDAV 密码已保存状态提示不同步；
- Web 小屏布局、长路径和操作按钮换行优化。

## 校验

Release 同时提供：

- `BiliPDJ-v2.0.2-Windows-Tk-Portable-x64.zip.sha256`
- `BiliPDJ-v2.0.2-Web-Portable-x64.zip.sha256`

可使用 SHA256 校验下载文件完整性。

## 升级说明

v2.0.1 用户建议完整解压 v2.0.2 到新目录，再迁移需要保留的配置文件。不要只复制单个 EXE，便携版需要保留 ZIP 解压后的完整目录结构。

---

版本号：`v2.0.2`  
发布日期：`2026-09-08`
