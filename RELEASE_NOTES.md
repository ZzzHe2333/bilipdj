# 弹幕排队姬 v2.0.3

v2.0.3 是基于 v2.0.2 的功能与使用体验更新版本，重点补齐 Web 本地控制面板、改进 Windows 原生 Bilibili 扫码登录与 Web 便携启动器，并统一 Windows / Web 的“关于”和“支持我们”页面。

## 本次提供的发行包

### Windows Tk 便携版

文件：`BiliPDJ-v2.0.3-Windows-Tk-Portable-x64.zip`

完整解压后双击 `main.exe`：

- 自动启动内置后端；
- 不需要安装 Python；
- 自带 Bilibili / 抖音后端、Web 静态资源、OBS 透明窗口和更新组件；
- Bilibili 扫码登录使用原生 Tk 二维码流程；
- 当前版本号、客户端类型和“关于”正文统一显示；
- 关闭桌面程序时，它启动的后端会一并停止。

### Web 便携版

文件：`BiliPDJ-v2.0.3-Web-Portable-x64.zip`

完整解压后双击 `BiliPDJ-Web.exe`：

- 自动检查并启动内置后端；
- 等待 `/health` 就绪后自动打开本地 Web 管理页；
- 新增完整本地控制面板，可统一管理日志、队列、平台和礼物设置、备份、OBS、权限、性能、更新与关于；
- 启动器后端进程默认隐藏运行，并提供更明确的状态显示；
- 浏览器控制页打开后启动器可自动最小化；
- 点击“停止并退出”只关闭本启动器自己拉起的后端；
- 不需要安装 Python，也不需要手工运行 Server 命令。

## Windows Bilibili 扫码登录改进

- Windows 客户端继续使用原生 Tk Bilibili 二维码登录，不依赖浏览器页完成扫码；
- 修复二维码轮询超时和终态处理；
- 扫码成功后同步展示并保存取得的 Cookie / UID；
- 在 Windows 客户入口显式安装原生扫码登录流程，避免打包后落回其他登录路径。

## Web 控制面板

新增统一的本地 Web 控制面板，覆盖：

- 日志查看；
- 队列管理；
- Bilibili / 抖音平台设置；
- 礼物与插队设置；
- 设置备份与恢复；
- OBS / 队列展示；
- 权限管理；
- 性能状态；
- 更新检查；
- 关于页面。

同时增加 API 路由覆盖检查和生成式 API 参考能力，减少前端与后端接口发生遗漏或漂移的风险。

## “支持我们”与前端提示

- Windows / Web 新增“支持我们”页面；
- 流量卡申请二维码由本地资源渲染；
- 提供对应申请入口；
- 随机推广提示仅注入前端显示层，不写入程序日志文件，不参与后端排队业务逻辑。

## “关于”页面统一

Windows 与 Web 统一为同一套说明正文，并分别显示客户端类型：

Windows：

```text
当前版本号：2.0.3
您的版本是：Windows版
```

Web：

```text
当前版本号：2.0.3
您的版本是：网页版
```

统一正文：

```text
Bilibili 直播弹幕排队管理工具
排队逻辑由 Python 后端统一处理，前端仅负责显示。

本软件完全免费，源码公开，Github Action自动打包，无后台无病毒，不损害电脑。若有人向你收费获取此软件（亲手帮安装调试除外），请立刻退款并举报！

【侵权/倒卖责任】
• 民事责任：侵权方须停止侵权、赔偿损失（含维权合理费用）。
• 刑事责任：以营利为目的的侵权行为，情节严重时可能被追究刑事责任。
```

## 设置备份

继续保留 v2.0.2 的设置备份能力，支持：

- WebDAV；
- 本地文件夹；
- SMB / NAS（使用操作系统已认证或已挂载的路径，不在 BiliPDJ 内保存 NAS 密码）。

备份范围固定为实际存在的以下设置文件：

- `config.yaml`
- `quanxian.yaml`
- `kaiguan.yaml`
- `style.json`

不会备份队列、黑名单、日志、WebDAV 密码或 NAS 凭据。ZIP 会保留每个设置文件的最后修改时间，恢复时同步恢复该时间。

## 其他调整

- 增加 MirrorChyan CDK 配置占位，当前仍保持 Mirror 更新功能禁用，不改变现有 GitHub Release 更新方式；
- Web 便携服务管理器进一步优化状态反馈与后台进程启动行为；
- Windows / Web 共享后端逻辑不变，前端仅负责操作和展示。

## 校验

Release 同时提供：

- `BiliPDJ-v2.0.3-Windows-Tk-Portable-x64.zip.sha256`
- `BiliPDJ-v2.0.3-Web-Portable-x64.zip.sha256`

可使用 SHA256 校验下载文件完整性。

## 升级说明

v2.0.2 及更早版本建议完整解压 v2.0.3 到新目录，再迁移需要保留的配置文件。不要只复制单个 EXE，便携版需要保留 ZIP 解压后的完整目录结构。

---

版本号：`v2.0.3`  
发布日期：`2026-09-08`
