# 弹幕排队姬 v1.0.9

v1.0.9 修复程序版本号与 GitHub Release 标签可能不一致的问题，统一版本来源并加强发布流程校验。

## 主要更新

### 1. 统一版本号来源

项目根目录新增 `VERSION` 文件，桌面控制台、关于页面和自动更新检查统一读取该文件，不再分别维护硬编码版本号。

当前版本：`1.0.9`。

### 2. 避免重复提示同一版本更新

旧实现中，如果发布标签已更新但程序内硬编码版本号未同步，安装新版后仍可能继续提示相同版本更新。

v1.0.9 改为从同一个 `VERSION` 文件读取版本，从源头消除该不一致。

### 3. 发布标签强制校验

Windows 和 macOS 发布工作流会在构建前检查：

- `VERSION` 必须符合数字版本格式；
- GitHub 标签必须严格等于 `v` 加 `VERSION`；
- 例如 `VERSION=1.0.9` 时，只允许发布 `v1.0.9`。

标签与版本不一致时，发布流程会立即停止，不再生成错误版本的安装包。

### 4. 打包程序包含 VERSION

Windows 和 macOS 的 PyInstaller 配置均会将 `VERSION` 文件打入程序包，确保源码运行和发行版运行使用相同版本信息。

### 5. 增加回归测试

新增版本来源测试，验证：

- `VERSION` 文件能被正确读取；
- 自动更新界面使用统一版本号；
- 后续修改版本时不会重新出现硬编码分叉。

## 升级说明

Windows v1.0.8 用户可以在程序“关于”页中检查并自动安装 v1.0.9。

也可以手动下载：

- `bilibili-danmuji-windows-x64-v1.0.9.zip`
- 对应的 `bilibili-danmuji-windows-x64-v1.0.9.zip.sha256`

macOS 用户下载与自身架构对应的发行包：

- Apple Silicon：`bilibili-danmuji-macos-arm64-v1.0.9.tar.gz`
- Intel：`bilibili-danmuji-macos-x86_64-v1.0.9.tar.gz`

---

版本号：`v1.0.9`  
主要修复：统一版本号来源与发布标签校验
