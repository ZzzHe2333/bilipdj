# 弹幕排队姬 v1.0.10

v1.0.10 是一次稳定性与安全性更新，集中修复自动更新、本地管理接口、队列并发、抖音参数兜底、透明窗口刷新和登录回调等问题。

## 主要更新

### 1. 自动更新与回滚可靠性

- 更新包解压或预检失败后会自动重新启动旧版本，不再出现程序关闭后没有恢复运行的情况。
- 新版本启动失败时继续执行完整回滚，并记录 `rolled_back` 状态。
- 更新成功后记录并安全清理临时下载目录，仅允许删除系统临时目录下由程序创建的更新目录。
- 暂存目录清理失败只写入日志，不再覆盖真正的更新错误。
- 自定义 OpenAI 兼容接口切换模型时保留已选择的 API 协议。

### 2. 本地管理接口与 WebSocket 加固

- 管理 API 增加本机 Host 校验，降低 DNS 重绑定风险。
- 浏览器写请求增加 Origin/Referer 同源校验。
- 请求体限制为 2 MiB，并拒绝非法 `Content-Length`。
- JSON 响应增加 `Cache-Control: no-store` 与 `X-Content-Type-Options: nosniff`。
- WebSocket 正确消费 Ping、Pong 和二进制帧，避免后续数据帧错位。
- 局域网客户端保持只读订阅权限，不能向其他客户端伪造队列消息。

### 3. 队列功能修复

- 修复“超级排”使用 `<用户名>` 表示时被误当作 HTML 标签删除的问题。
- 修复礼物插队资格与舰长插队资格同时生效时，同一用户可能重复入队的问题。
- 统一配置锁与队列锁的获取顺序，避免管理页面与弹幕指令并发时发生永久死锁。
- 保留普通 HTML 清洗规则，超级排修复不会放宽前端内容安全限制。

### 4. 抖音监听兼容性

- 修复页面参数不完整时，已保存的 `room_id` 和用户 ID 兜底逻辑实际不可达的问题。
- 宽松解析仅在抖音监听线程中启用，桌面参数获取和健康检查仍保持严格校验。
- 使用线程局部状态隔离解析模式，不影响其他线程。

### 5. 透明窗口稳定性

- 后台 HTTP 请求线程不再直接调用 Tk/Tcl。
- 刷新结果通过线程安全队列交回主线程，再执行重绘和下一次刷新。
- 关闭透明窗口时取消轮询和定时任务，避免窗口关闭后刷新线程异常或永久卡住。

### 6. 扫码登录回调安全

- 公网回调地址必须使用 HTTPS；仅本机回环地址允许 HTTP。
- 禁止回调 URL 携带用户名或密码。
- 禁止凭据回调自动跟随重定向，防止 Cookie 和 Bearer Token 被转发到其他地址。
- 回调超时限制为 1～30 秒。

### 7. 回归测试与发布流程

新增或补充以下测试：

- 自动更新预检失败重启与临时目录清理；
- 本地 API、WebSocket 和登录回调安全策略；
- 抖音预存参数兜底及线程隔离；
- 超级排、礼物插队与舰长插队叠加；
- 配置锁与队列锁顺序；
- 透明窗口主线程刷新；
- 自定义模型 API 协议保持。

Windows 发布工作流现在会在 `now` 分支的 `VERSION` 文件变更时自动构建并发布对应版本，同时继续支持手动触发和标签触发。

## 升级说明

Windows v1.0.8 或 v1.0.9 用户可在程序“关于”页检查并自动安装 v1.0.10。

手动下载文件：

- `bilibili-danmuji-windows-x64-v1.0.10.zip`
- `bilibili-danmuji-windows-x64-v1.0.10.zip.sha256`

---

版本号：`v1.0.10`  
发布日期：`2026-07-30`
