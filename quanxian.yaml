# 权限配置
# 最高管理员：拥有所有权限，包括新增/删除管理员
super_admin:
  - "一纸轻予梦"

# 管理员：拥有除新增/删除管理员以外的所有操作权限
admin:

# 舰长：仅拥有「插队」命令权限
jianzhang:

# 成员：普通观众，任何人都可以是成员，成员没有任何权限限制，不在这里的就添加进这里，在不在这个名单对使用命令没有任何影响
member:

# 白名单: 仅白名单排队生效的时候，允许使用命令的用户列表（不在白名单中的用户将被禁止使用任何命令，管理员除外）
whitelist:

# 黑名单：禁止使用任何命令
blacklist:    
