# 多平台弹幕监听

本功能允许弹幕排队姬同时监听多个已实现的直播平台。当前并行 Relay 支持：

- B站（Bilibili）
- 抖音（Douyin）

其他平台仍保持原来的预留状态，后续实现各自 Relay 后可以继续接入聚合层。

## 开启方式

桌面控制台 **设置 → 多平台弹幕** 中：

1. 勾选 **启用多平台弹幕监听**。
2. 勾选需要同时监听的平台（B站 / 抖音）。
3. B站需要有效直播间号。
4. 抖音需要在“平台参数”中开启“启用抖音配置”，并填写有效 `live_id`。
5. 保存配置并启动/重启后端。

只有至少两个已实现的平台同时满足配置条件时，后端才会创建多平台聚合 Relay；否则继续沿用原来的单平台监听逻辑。

多平台开关单独保存在运行目录的 `multi_platform_danmu.json`，默认关闭，因此升级后不会改变现有单平台行为。

## 弹幕触发

多平台模式下，各平台拥有独立的弹幕连接线程，但最终进入同一个 `QueueManager`，因此两路弹幕都能触发现有排队、取消、修改和管理员命令。

例如 B站和抖音同时在线时：

```text
B站用户 A：排队
抖音用户 B：排队
B站管理员：完成
抖音管理员：删除 2
```

都会进入同一套队列逻辑。

## 平台用户标识符

每条弹幕都会生成内部用户标识：

```text
bilibili:uid:123456

douyin:sec_uid:MS4wLjAB...
```

字段同时写入弹幕事件：

- `platform`
- `platform_user_key`
- `internal_user_id`
- `identity.original_uid`
- `identity.runtime_uid`

抖音优先使用 `sec_uid`；没有 `sec_uid` 时使用平台 UID；如果都没有，则使用平台名 + 用户名哈希作为兜底。

为了防止不同平台 UID 数字碰撞，非 B站平台进入排队逻辑时会使用稳定的 namespaced `runtime_uid`。B站继续保留原 UID，以兼容礼物排队资格。

## 同名用户

队列界面仍然显示原来的用户名，不强制添加平台前缀；内部另外维护 `platform_user_key`。

因此：

```text
B站：张三
抖音：张三
```

可以同时存在于队列中，并分别执行取消/修改等针对自己的操作。

当前平台用户键属于运行时队列元数据。旧版本 CSV 存档没有该字段时仍可正常读取；恢复后的旧条目会在对应用户下一次发送命令时自动绑定平台身份。

## 日志来源

弹幕日志会明确标记来源：

```text
[弹幕][B站] 张三(普通用户) user_key=bilibili:uid:123: 排队
[弹幕][抖音] 张三(普通用户) user_key=douyin:sec_uid:xxx: 排队
```

触发命令和提示日志也会带来源平台：

```text
[触发指令][B站] ...
[提示][抖音] ...
```

WebSocket 的 `DANMU_EVENT` 同样包含来源平台和内部用户标识，可供后续 UI、统计或跨平台权限扩展使用。
