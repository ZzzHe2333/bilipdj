# 管理员细分权限

Bilipdj 支持由最高管理员为普通管理员分配细粒度管理权限。最高管理员和主播始终拥有完整权限，不受此配置限制。

## 可分配权限

| 权限 | 对应操作 |
|---|---|
| 删除 / 完成队列项 | `完成`、`del`、`删除` |
| 手动新增队列项 | `add`、`新增`、`添加` |
| 指定位置插队 / 无影插 | `插队 [位置] [内容]`、`无影插 [位置] [内容]` |
| 暂停 / 恢复排队功能 | 暂停、关闭、恢复自助排队 |
| 修改排队人数上限 | `设置排队人数`、`设置排队上限` |
| 舰长插队开关 | 开启 / 关闭舰长插队 |
| 房管管理权限开关 | 允许 / 停止房管成为插件管理员 |
| 黑名单管理 | `拉黑`、`取消拉黑` |

## 桌面控制台

在 **权限** 页面维护管理员名单后，可以在下方的 **管理员细分权限（由最高管理员配置）** 区域选择一个管理员，并勾选该管理员允许执行的操作。

- 未设置单独限制：保持旧版本行为，拥有完整管理员权限。
- 勾选一项或多项并保存：只允许选中的管理操作。
- 取消全部勾选并保存：仍保留 `admin` 身份，但不能执行任何管理指令。
- 点击 **恢复完整权限**：删除该管理员的单独限制，恢复旧版完整权限。

细分权限保存在本地 `admin_permissions.json` 中。源码运行时位于 `core/`，打包运行时位于主程序同级目录。该文件属于本地运行配置，不应提交到仓库。

## 弹幕命令

最高管理员还可以直接通过弹幕调整普通管理员权限：

```text
设置管理员权限 管理员A 删除,黑名单
设置管理员权限 管理员A 全部
清空管理员权限 管理员A
重置管理员权限 管理员A
查看管理员权限 管理员A
```

权限名称支持常用中文别名，例如：`删除`、`新增`、`插队`、`排队开关`、`排队上限`、`舰长开关`、`房管权限`、`黑名单`。

其中：

- `清空管理员权限` 会保留管理员身份，但禁止全部管理指令。
- `重置管理员权限` 会移除单独限制，使该管理员重新拥有完整管理员权限。
